{
  "name": "@storacha/fc-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "pnpm build && mkdir -p dist/contracts && cp src/contracts/*.json dist/contracts/ && node --test dist/test/**/*.test.js",
    "test:calibration": "pnpm build && mkdir -p dist/contracts && cp src/contracts/*.json dist/contracts/ && node dist/scripts/calibration-smoke.js"
  },
  "dependencies": {
    "@filoz/synapse-core": "^0.1.4",
    "@filoz/synapse-sdk": "^0.36.1",
    "@ucans/ucans": "^0.12.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "ethers": "^6.11.1",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/multer": "^1.4.11",
    "@types/node": "^20.11.5",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  }
}
